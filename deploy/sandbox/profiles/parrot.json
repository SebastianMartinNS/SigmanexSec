{
  "$schema": "./profile.schema.json",
  "name": "parrot",
  "description": "Generic Parrot OS catalog dispatcher — used when a tool does not fit the recon/exploit/osint taxonomy (e.g. forensic helpers, decoders). Conservative default profile: RO root, /work spill, network egress kept open and bounded by scope_validator.",
  "bind_ro": ["/usr", "/etc", "/lib", "/lib64", "/bin", "/sbin"],
  "workdir_template": "/tmp/sap-{run_id}/parrot",
  "workdir_mount": "/work",
  "tmpfs": ["/tmp", "/var/tmp"],
  "dev": true,
  "proc": true,
  "as_pid_1": true,
  "unshare_user": true,
  "unshare_pid": true,
  "unshare_ipc": true,
  "unshare_uts": true,
  "network_policy": "in_scope",
  "caps_drop": ["ALL"],
  "caps_keep": []
}
