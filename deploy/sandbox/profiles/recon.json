{
  "$schema": "./profile.schema.json",
  "name": "recon",
  "description": "Reconnaissance tools (nmap, masscan, dnsrecon, dnsenum, whatweb, nikto, gobuster, ffuf, feroxbuster, dirb, wfuzz, dirsearch, wafw00f, enum4linux, nbtscan, snmp-check, onesixtyone). Read-only root, ephemeral per-run /work, network egress kept open so scope_validator remains the network policy enforcement point.",
  "bind_ro": ["/usr", "/etc", "/lib", "/lib64", "/bin", "/sbin"],
  "workdir_template": "/tmp/sap-{run_id}/recon",
  "workdir_mount": "/work",
  "tmpfs": ["/tmp", "/var/tmp"],
  "dev": true,
  "proc": true,
  "as_pid_1": true,
  "unshare_user": true,
  "unshare_pid": true,
  "unshare_ipc": true,
  "unshare_uts": true,
  "network_policy": "in_scope",
  "caps_drop": ["ALL"],
  "caps_keep": []
}
